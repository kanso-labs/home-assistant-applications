# API keys for Configarr, referenced from config.yml with !secret.
#
# Seeded on first start and never overwritten afterwards. Each arr application
# prints its own key under Settings, then General.

RADARR_API_KEY: replace-with-the-radarr-api-key
SONARR_API_KEY: replace-with-the-sonarr-api-key

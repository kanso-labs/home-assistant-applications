arch:
  - aarch64
  - amd64
backup: cold
backup_exclude:
  - '**/Plex Media Server/Cache/**'
  - '**/Plex Media Server/Crash Reports/**'
  - '**/Plex Media Server/Diagnostics/**'
  - '**/Plex Media Server/Logs/**'
  - '**/Plex Media Server/Media/**'
  - '**/Plex Media Server/Metadata/**'
  - '**/Plex Media Server/Plug-in Support/Caches/**'
description:
  Recorded media, live TV, online news, and podcasts ready to stream. Plex Media
  Server organises what you already have and serves it to the Plex apps.
devices:
  - /dev/dri
  - /dev/dri/card0
  - /dev/dri/card1
  - /dev/dri/renderD128
image: 'ghcr.io/kanso-labs/home-assistant-application-plex-media-server'
init: false
map:
  # Supervisor deprecated `addon_config` for `app_config` in 2026.07, but
  # frenck/action-addon-linter still only accepts the older spelling and has
  # had no release since 2025-11. Home Assistant's own apps-example runs the
  # same linter. Switch this once the linter's schema catches up.
  - read_only: false
    type: addon_config
  - read_only: true
    type: media
  - read_only: true
    type: share
name: Plex Media Server
options:
  allowed_networks: []
  claim_code: ''
ports:
  32400/tcp: 32400
  32410/udp: 32410
  32412/udp: 32412
  32413/udp: 32413
  32414/udp: 32414
  32469/tcp: 32469
  8324/tcp: 8324
ports_description:
  32400/tcp: Web interface and API
  32410/udp: Discovery on the local network
  32412/udp: Discovery on the local network
  32413/udp: Discovery on the local network
  32414/udp: Discovery on the local network
  32469/tcp: DLNA server
  8324/tcp: Plex Companion, for controlling playback from another device
schema:
  allowed_networks:
    - str
  claim_code: str?
slug: plex-media-server
url: 'https://github.com/kanso-labs/home-assistant-applications/tree/main/plex-media-server'
version: '1.1.1' # x-release-please-version
webui: 'http://[HOST]:[PORT:32400]/web'
